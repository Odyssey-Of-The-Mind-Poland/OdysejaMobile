import 'package:flutter/material.dart';

class ScheduleSearchScreen extends StatelessWidget {
  const ScheduleSearchScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
